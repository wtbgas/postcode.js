/** typings for PostCode
  * usage: 
  * import PostCode  = require('postcode');
  * var postCode = new PostCode('A12 B23');
  */

declare function PostCode(postCode: string): void;
export = PostCode;
